@model IEnumerable<v8.Models.ReparationIntervention>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReparationInterventionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reparation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Intervention)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReparationInterventionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reparation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Intervention.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
